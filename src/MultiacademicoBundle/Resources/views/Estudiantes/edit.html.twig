{% block content -%}
<div id="content">
    <div class="row">
        <big-breadcrumbs items="['Estudiantes', 'Editar']" icon="file"
                         class="col-xs-12 col-sm-7 col-md-7 col-lg-4"></big-breadcrumbs>
            </div>
    
    <div class="row">
    {{ form(edit_form) }}
    </div>
   <div  class="record_actions" style="display:-webkit-box;">
            <a class="btn btn-success" href="{{ path('estudiantes') }}">
            Regresar a la lista
            </a>                         
            {{ form_start(delete_form) }}
             {{ form_widget(delete_form.submit,{'attr':{'class':'btn-danger'} }) }}                       
            {{ form_end(delete_form) }}                
        </div>
</div>
{% endblock %}
