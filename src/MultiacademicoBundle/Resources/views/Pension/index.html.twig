{% extends 'base.html.twig' %}

{% block body %}
        <style>
        label{
            display: table;
        }
        div.dataTables_filter input {
          margin-left: 0px; 
        }
        .dataTable thead tr th:first-child{
          min-width: 10px;  
        }

    </style>
    <h1>Pensiones listado</h1>
    
    <div class="row">
        <div class="col-sm-12">
        
             <div class="col-sm-6 btn-group">
                 <button href="{{path('pensiones_download')}}" rel='tooltip' title ='Descargar Archivo para Banco' class = 'btn btn-lilac'   target= '_blank'  role = 'button'>
                    <i class='fa fa-bank'/>Exportar a Banco
                 </button>
                <button type="button" class="btn btn-lilac dropdown-toggle" data-toggle="dropdown" aria-expanded="false" aria-haspopup="true">
                                                <span class="caret"></span>
                 </button> 
                <ul class="dropdown-menu" role="menu" >
                    <li>
                        <a href="{{path('pensiones_download',{'seccion':'matricula'})}}" target="_blank" ><i class="fa fa-bank"></i> Matriculas</a>
                    </li>
                    <li class="divider"></li>
                    <li>
                        <a href="{{path('pensiones_download',{'seccion':'mayo'})}}" target="_blank"> Mayo</a>
                    </li>
                    <li>
                        <a href="{{path('pensiones_download',{'seccion':'junio'})}}" target="_blank"> Junio</a>
                    </li>
                    <li>
                        <a href="{{path('pensiones_download',{'seccion':'julio'})}}" target="_blank"> Julio</a>
                    </li>
                    <li>
                        <a href="{{path('pensiones_download',{'seccion':'agosto'})}}" target="_blank"> Agosto</a>
                    </li>
                    <li>
                        <a href="{{path('pensiones_download',{'seccion':'septiembre'})}}" target="_blank"> Septiembre</a>
                    </li>
                    <li>
                        <a href="{{path('pensiones_download',{'seccion':'octubre'})}}" target="_blank"> Octubre</a>
                    </li>
                    <li>
                        <a href="{{path('pensiones_download',{'seccion':'noviembre'})}}" target="_blank"> Noviembre</a>
                    </li>
                    <li>
                        <a href="{{path('pensiones_download',{'seccion':'diciembre'})}}" target="_blank"> Diciembre</a>
                    </li>
                    <li>
                        <a href="{{path('pensiones_download',{'seccion':'enero'})}}" target="_blank"> Enero</a>
                    </li>
                    {#<li>
                        <a ng-href="{{'{{urlAnexo('~entity.cedula~')}}'}}" target="_blank">Anexo</a>
                    </li>
                    
                    <li class="divider"></li>
                    <li>
                        <a href-void="" href="#">Separated link</a>
                    </li>#}
                </ul>
            </div>
                
            <div class="col-sm-6">
                {{ form_start(form) }}
                    {{ form_widget(form) }}
                    <input class="btn btn-lilac" type="submit" value="Subir" />
                {{ form_end(form) }}
            </div>    
            
            
       
        </div>
    </div>
        {{ datatable_render_html(datatable) }}

    {{ datatable_render_js(datatable) }}
{% endblock %}
