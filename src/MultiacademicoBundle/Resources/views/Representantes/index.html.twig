{% extends 'base.html.twig' %}

{% block body %}
    <style>
        label{
            display: table;
        }
        div.dataTables_filter input {
          margin-left: 0px; 
        }
        .dataTable thead tr th:first-child{
          min-width: 10px;  
        }
    </style>
    <h1>Representantes listado</h1>

        {{ datatable_render_html(datatable) }}
    <ul>
        <li>
            <a href="{{ path('representantes',{'page':'new'}) }}">Crear nueva entrada</a>
        </li>
    </ul>   {{ datatable_render_js(datatable) }}
{% endblock %}
