{% extends app.request.isXmlHttpRequest ? '::baseajaxforms.html.twig' : '::basesmartpanel.html.twig' %}
{% form_theme edit_form 'MultiservicesArxisBundle:Form:campos.html.twig' %}
{% form_theme delete_form 'MultiservicesArxisBundle:Form:campos.html.twig' %}
{% block content -%}
    <h1>User edit</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('secured_user') }}">
            Regresar a la Lista
        </a>
    </li>
    <li>{{ form_start(delete_form) }}{{ form_row(delete_form.submit, { 'label': 'Eliminar'}) }}{{ form_end(delete_form) }}</li>
</ul>
{% endblock %}
